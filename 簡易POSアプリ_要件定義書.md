# 簡易POSアプリ（初級編）要件定義書

*―Lv1要求一覧に基づく要件定義―*

| 項目 | 内容 |
|---|---|
| 文書名 | 簡易POSアプリ（初級編）要件定義書 |
| 版数 | v1.0（ドラフト） |
| 作成日 | 2026年9月1日 |
| 作成者 | 橋本 悠保 |
| 元資料 | Lv1_簡易POSアプリ初級編_要求一覧.docx |
| 提出先 | 講師 |

---

## 1. 本書について

本書は、「Lv1_簡易POSアプリ初級編_要求一覧」（以下「Lv1要求一覧」）に記載された要求（＝何を作るべきか）を分析・整理し、設計・実装フェーズに引き継ぐための要件定義書である。

Lv1要求一覧はAPI仕様やデータベース設計といった実装方式（＝どう作るか）を意図的に含めていない。本書では、要求から論理的に導かれる範囲で機能要件・非機能要件・データ要件を具体化し、Lv1要求一覧が開発チームの判断に委ねている技術方針（4章）についても、その判断結果と理由を明記する。ただし、API仕様・画面遷移の詳細・データベーステーブル設計などの実装設計そのものは、本書のスコープ外とし、後続の設計書に委ねる。

対象読者は本演習の講師、および本要件定義書を引き継いで設計・実装を行う開発チーム自身を想定する。

## 2. 用語定義

| 用語 | 説明 |
|---|---|
| 商品マスタ | 商品コード・名称・単価などの商品情報を保持する、本システム外で管理された既存データ。 |
| 会員マスタ | 会員ID・氏名・電話番号・住所・性別・年齢などの会員情報を保持する、本システム外で管理された既存データ。 |
| 担当者 | レジ業務を行うため本システムにログインするレジ担当者。担当者IDとパスワードで認証される。 |
| 購入リスト | 会員識別後、確定前の状態で画面上に保持される、登録済み商品（名称・数量・単価・小計）の一覧。 |
| 購入確定 | 購入リストの内容を確定し、購入記録としてデータベースに永続化する操作。 |
| 税込金額 / 税抜金額 | 消費税を含む合計金額 ／ 消費税を含まない合計金額。 |
| 消費税率データ | 消費税率の値を、プログラムコードを変更せずに更新できる形で保持したデータ。 |
| お客様ID読み込みボタン | 会員カードを保有しない客が、会員IDを入力せずに商品登録・購入へ進むために押下する操作（画面上のボタン）。Lv1要求一覧の呼称をそのまま用いている。 |

## 3. 前提条件

以下はLv1要求一覧において変更不可の前提条件として明示されているものである。

- Webアプリケーションとして動作すること（ブラウザからアクセスする）。
- フロントエンドはNext.jsで実装する。
- バックエンドはFastAPIで実装する。
- インフラはMicrosoft Azure上に構築する。
- データベースはAzure Database for MySQL Flexible Serverを利用する。

加えて、本要件定義書の作成にあたり、開発チームのヒアリングにより以下を前提とする（詳細な判断理由は付録Aを参照）。

- 商品マスタ・会員マスタは本システム外に既に存在するものとし、本システムはこれらを参照するのみで、登録・編集・削除は行わない（8章「スコープ外事項」参照）。

## 4. システム構成方針（アーキテクチャ前提）

Lv1要求一覧は、非機能要求として「営業時間内は常に同じような応答速度で動作すること」「バックエンドはDBとの接続をある程度維持した状態で運用することが望ましい」を挙げたうえで、バックエンドをAzure上のどのサービスで動かすかを開発チームの判断に委ねている。本要件定義書では、これらの非機能要求を満たす方針として以下を採用する。

- バックエンドの実行基盤にはAzure App Serviceを採用する。常時起動型のサービスであるため、コールドスタート（アクセスが久しぶりにあった際の応答遅延）を避けやすく、アプリケーション内でデータベース接続をある程度維持する構成とも相性がよい。
- フロントエンド（Next.js）とバックエンド（FastAPI）は、REST API等のAPI連携によって役割分担する。
- 消費税率は、アプリケーションのプログラムコードに直接埋め込まず、コードを変更せずに更新できる形でデータとして保持する。具体的な保持方法（データベースのテーブルとするか、設定ストアとするか等）は設計フェーズで決定する。

> 注記：本方針（Azure App Serviceの採用）は、要件定義書の中で扱うべき「方針」の粒度として記載したものであり、具体的なプラン・スケール設定・デプロイ方式などの詳細設計は後続の設計書に委ねる。

## 5. 機能要件

### 5.1 ログイン機能

- **FR-1-1**：レジ担当者は、担当者IDとパスワードを用いて、ログイン専用画面からログインする。
- **FR-1-2**：ログインに成功した場合、POSアプリ画面（商品登録・購入画面）に遷移する。
- **FR-1-3**：担当者IDまたはパスワードが誤っている場合、ログインを許可せず、エラーである旨を表示する。
- **FR-1-4**：認証は本システム単独で行い、社外の統合ID基盤（会社の統一ログインの仕組み等）とは連携しない。
- **FR-1-5**：ログインした担当者の識別情報は、その後の取引（購入確定）記録に関連付けられる（5.5節参照）。

### 5.2 会員識別機能

- **FR-2-1**：商品登録を開始する前に、会員IDを入力するステップを設ける。
- **FR-2-2**：会員カードを保有する客の会員IDは、手入力によって受け付ける。
- **FR-2-3**：入力された会員IDに一致する会員が会員マスタに存在する場合、その会員として以降の処理（商品登録・購入）に進む。
- **FR-2-4**：入力された会員IDに一致する会員が存在しない場合、エラーを表示し、会員IDの再入力を求める（商品登録・購入には進めない）。
- **FR-2-5**：会員カードを保有しない客は、会員IDを入力せず「お客様ID読み込みボタン」を押下することで、会員IDなしの状態のまま商品登録・購入に進める。
- **FR-2-6**：会員マスタ（会員ID・氏名・電話番号・住所・性別・年齢）は参照のみとし、本システムから登録・編集・削除は行わない（8章参照）。

### 5.3 商品検索機能

- **FR-3-1**：画面には商品コードを入力する項目と、入力した商品コードで商品を検索する操作を設ける。
- **FR-3-2**：入力された商品コードに一致する商品が商品マスタに存在する場合、当該商品の名称・単価を画面に表示する。
- **FR-3-3**：入力された商品コードに一致する商品が存在しない場合、「商品がマスタ未登録です」旨をユーザーに伝える。
- **FR-3-4**：商品マスタは参照のみとし、本システムから登録・編集・削除は行わない（8章参照）。

### 5.4 購入リスト管理機能

- **FR-4-1**：検索により名称・単価が表示された商品を、購入リストに追加する操作を設ける。
- **FR-4-2**：購入リストには、追加された商品の名称・数量・単価・小計を一覧表示する。
- **FR-4-3**：同一の商品コードが複数回登録された場合、購入リスト上で別行にはせず、既存の1行の数量に加算する（1商品1行の原則）。
- **FR-4-4**：商品を購入リストに追加すると、商品コード入力欄・名称表示・単価表示の内容をクリアし、次の商品を続けて登録できる状態に戻す。
- **FR-4-5**：商品登録（検索→追加）は、購入確定前であれば何度でも繰り返せる。
- **FR-4-6**：購入確定前であれば、購入リストから商品を削除する操作、および数量を変更する操作を行える。
- **FR-4-7**：削除・数量変更を行った場合、対象商品の小計および購入リスト全体の合計金額に即時反映する。
- **FR-4-8**：購入リストから削除した商品コードは、商品検索からあらためて検索・追加し直すことができる（登録できる回数の制限は設けない）。
- **FR-4-9**：数量は1以上の整数とする。数量を0以下にする操作が行われた場合は、当該商品を購入リストから削除したものとして扱う。

> 注記：FR-4-6〜FR-4-9はLv1要求一覧に明記がなく、ヒアリングおよび本書作成時の検討により追加した要件である（付録A参照）。

### 5.5 購入確定機能

- **FR-5-1**：購入リストの内容を確定する操作を設ける。
- **FR-5-2**：購入確定時、購入結果をデータベースに永続化する。
- **FR-5-3**：保存する購入記録には、少なくとも「いつ（日時）・何を（商品）・いくつ（数量）・いくらで（金額）・誰が買って（会員ID、会員なしの場合はその旨）・誰がレジ処理をしたか（担当者ID）」を後から特定できる情報を含める。
- **FR-5-4**：購入確定後、税込みの合計金額をポップアップ等でユーザーに表示する。
- **FR-5-5**：上記ポップアップには、税込み金額に加えて税抜き金額もあわせて表示する。
- **FR-5-6**：ユーザーがポップアップを閉じる操作を行うと、商品コード入力・名称表示・単価表示・購入リストの内容をクリアし、次の会員識別（5.2節）から再開できる状態に戻す。

### 5.6 消費税率管理機能

- **FR-6-1**：消費税率は、アプリケーションのプログラムコード内に固定値として埋め込まない。
- **FR-6-2**：消費税率は、プログラムコードを変更せずに更新できる形でデータとして保持する（保持方法の詳細は4章および設計フェーズで決定）。
- **FR-6-3**：購入金額の計算（税込み・税抜き）は、上記の消費税率データを参照して行う。

## 6. 非機能要件

6.1〜6.3はLv1要求一覧の非機能要求から導かれる確定要件である。6.4は本書作成時にチームが補足した提案であり、本書の時点では未確定である（10章参照）。

### 6.1 性能・可用性

- **NFR-1-1**：レジとしての利用中（営業時間内）は、アクセス頻度によらず一定した応答速度を維持すること。しばらくアクセスがなかった直後に極端に応答が遅くなるという挙動を許容しない。
- **NFR-1-2**：上記を実現するため、バックエンドの実行基盤には常時稼働型のサービスを採用し、コールドスタートが発生しやすい構成（例：従量課金プランのサーバーレス関数など）は避ける（4章参照）。

### 6.2 データ整合性・永続性

- **NFR-2-1**：購入確定によって保存された購入データは、失われてはならない。
- **NFR-2-2**：バックエンドとデータベースの接続は、リクエストのたびに一から確立するのではなく、ある程度維持した状態で運用する（コネクションプーリング等の具体的方式は設計フェーズで決定）。

### 6.3 保守性

- **NFR-3-1**：消費税率の変更は、プログラムの改修・再デプロイを伴わずに反映できること。

### 6.4 セキュリティに関する提案事項（未確定）

- **提案-1**：担当者のパスワードは、平文で保存せず、安全な方式（ハッシュ化等）で保管すること。

> 注記：この項目はLv1要求一覧に明記されていないため、6.1〜6.3とは異なり正式なNFR番号を付与していない。認証機能を持つシステムとして一般的に満たすべき事項として本書作成時に補足した提案であり、要否・要求への追加可否について講師・チームで確認をお願いしたい（10章参照）。

## 7. データ要件

以下は本システムが保持・参照すべきデータ項目である。具体的なテーブル構造・データベーススキーマの設計は本書のスコープ外とし、設計フェーズで定める。

| データ | 主な項目 | 取扱い |
|---|---|---|
| 商品情報 | 商品コード、名称、単価 | 参照のみ（既存の商品マスタ） |
| 会員情報 | 会員ID、氏名、電話番号、住所、性別、年齢 | 参照のみ（既存の会員マスタ） |
| 担当者情報 | 担当者ID、パスワード | 参照・認証に利用（新規登録機能はスコープ外。8章参照） |
| 消費税率情報 | 適用税率の値 | コードを変更せず更新可能な形で保持（4章参照） |
| 購入記録 | 購入日時、会員ID（またはなしの旨）、担当者ID、購入明細（商品ごとの名称・数量・単価・小計）、合計金額（税込み・税抜き） | 購入確定時に新規保存（更新・削除は想定しない） |

## 8. スコープ外事項

Lv1要求一覧に記載がなく、本書においても対象範囲に含めないことを明記する事項は以下のとおりである。

- 商品マスタの登録・編集・削除機能
- 会員マスタの登録・編集・削除機能
- 担当者情報の新規登録・編集機能（担当者IDとパスワードの初期登録は本システム外で行われる前提とする）
- 返品・取消処理、レシート／領収書の印刷機能
- 軽減税率など複数の消費税率区分への対応（単一の消費税率が適用されることを前提とする）
- 担当者のログアウト機能、パスワード変更機能
- 現金・カード等の決済手段の選択・分岐処理

## 9. 受け入れ基準

各機能要件について、以下の観点をもって受け入れ可否を判定する。

| 対象 | 受け入れ基準 |
|---|---|
| ログイン（5.1） | 正しい担当者ID・パスワードでログインできること。誤った入力ではログインが拒否され、エラーが表示されること。 |
| 会員識別（5.2） | 登録済み会員IDで先に進めること。未登録の会員IDではエラーとなり先に進めないこと。会員IDなしでも「お客様ID読み込みボタン」経由で先に進めること。 |
| 商品検索（5.3） | 登録済み商品コードで名称・単価が表示されること。未登録コードでは「商品がマスタ未登録です」等のメッセージが表示されること。 |
| 購入リスト（5.4） | 同一商品を複数回登録しても1行に数量集約されること（FR-4-3）。削除・数量変更が確定前に行え、小計・合計に即時反映されること（FR-4-6・FR-4-7）。削除した商品を再登録できること（FR-4-8）。数量を0以下にする操作は削除として扱われること（FR-4-9）。 |
| 購入確定（5.5） | 確定操作でDBに購入記録が保存されること。税込み・税抜きの合計がポップアップ等で表示され、閉じると画面が初期状態に戻ること。 |
| 消費税率（5.6） | 税率変更がコード修正なしに反映されること（設定変更のみで金額計算に反映されることを確認する）。 |
| 性能（6.1） | 一定時間アクセスがない状態からのアクセスでも、通常時と同程度の応答時間で動作すること。 |
| データ永続性（6.2） | 購入確定後にシステムを再起動しても、保存済みの購入記録が失われていないこと。 |

## 10. 未解決事項・要確認事項

- 6.4節のセキュリティ要件（パスワードの安全な保管）を、正式な要件として追加してよいか、講師・チームでの確認をお願いしたい。
- 消費税率データの具体的な保持先（データベースの設定テーブルとするか、アプリケーションの設定ストアとするか等）は、設計フェーズで確定する。
- 購入リストの削除・数量変更（5.4節、FR-4-6〜FR-4-9）に関する画面操作の詳細（確認ダイアログの要否等）は、設計フェーズで定める。
- 応答速度に関する具体的な数値基準（許容応答時間等、6.1節）は本書では定量化していない。必要に応じて講師・チームで基準値を検討する。
- 担当者情報（担当者ID・パスワード）の初期登録がどこで、どのように行われるかは本書では明確にしていない。運用開始前の準備方法について確認が必要である。
- ログアウト機能をスコープ外としているため（8章）、担当者交代時の運用方法（ログイン状態の扱い等）について、業務ルールとして別途整理する必要がある。

## 11. 変更履歴

| 版数 | 日付 | 変更内容 |
|---|---|---|
| v1.0 | 2026-09-01 | Lv1要求一覧をもとに初版を作成 |

---

## 付録A：Lv1要求一覧の解釈・判断ログ

Lv1要求一覧が開発チームの判断に委ねている論点、および記載が曖昧なため本要件定義書の作成過程でヒアリングにより決定した論点を、判断内容と理由とともに記録する。

| 論点 | 決定内容 | 理由 |
|---|---|---|
| バックエンドの実行基盤（Azure上のどのサービスを使うか） | Azure App Serviceを採用する。 | 「営業時間内は常に同じ応答速度」「DB接続をある程度維持」という非機能要求を満たすため、常時稼働型のサービスを選び、コールドスタートが起きやすいサーバーレス構成を避けた。 |
| 入力された会員IDに一致する会員が存在しない場合の挙動 | エラーを表示し、再入力を求める（先に進めない）。 | Lv1要求一覧が「考慮する必要がある」とするのみで挙動を定めていなかったため、商品検索での「マスタ未登録」時の扱いと一貫させ、誤った会員IDのまま取引が進むことを防ぐ方針とした。 |
| 商品マスタ・会員マスタの登録・編集機能の要否 | 対象外とし、参照のみ行う。 | Lv1要求一覧に「登録済みの前提である」等の記載はあるが、登録・編集画面についての言及がないため、既存データを前提とし、本アプリのスコープは登録処理を含まないと判断した。 |
| 購入リストに追加した商品の削除・数量変更（確定前）の可否 | 可能とする（FR-4-6〜FR-4-9として要件に追加）。 | Lv1要求一覧には明記がないが、レジ業務の実用上、誤って登録した商品を確定前に訂正できないと運用に支障が出ると判断し、要件として追加した。 |
